# Отчёт по ЛР 1

Студент: **Либченко Михаил Вадимович**, **M4206**

Алгоритм: **PPMd — Prediction by Partial Matching, вариант D + Арифметическое (Range) кодирование**

## Запуск

> Для запуска необходимо скачать `.exe`, соответствующий платформе запуска
> на вкладке [releases](https://github.com/lipa44/huffman-archivator/releases)

```shell
./Archivator.exe [encoder|decoder] [inputPath] [outputPath]
```

## Описание алгоритма

PPMd (Prediction by Partial Matching, variant D) — статистический метод сжатия данных, основанный на
контекстном моделировании. Для каждого байта входного потока алгоритм анализирует предшествующие N байт
(контекст порядка N) и оценивает вероятности появления следующего символа. Полученные вероятности подаются
на арифметический (range) кодер, который кодирует символы практически оптимально — без округления до целых бит,
свойственного кодированию Хаффмана.

Ключевые особенности реализации:

- **Адаптивная модель** — статистика накапливается по мере обработки данных, начиная с пустой модели
- **Method D для оценки escape** — при спуске к контекстам меньшего порядка
- **Update exclusion** — символы, встреченные в контексте более высокого порядка, исключаются из рассмотрения в
  контекстах меньшего порядка
- **Настраиваемый порядок модели** — по умолчанию 6, передаётся через конструктор

## Энкодер

### Процесс кодирования

Кодирование выполняется конвейером из двух этапов: контекстное моделирование (PPM) и арифметическое кодирование.

#### 1. Контекстная модель PPM

Модель хранит дерево контекстов (trie) от порядка 0 до порядка N (задаётся параметром `modelOrder`).
Каждый узел дерева содержит статистику частот символов, встреченных после данного контекста.

Для каждого входного байта выполняется следующий процесс:

1. **Поиск символа в контекстах от максимального к минимальному порядку:**
    - Начиная с контекста порядка N (самого длинного), проверяется, встречался ли текущий символ в данном контексте
    - Если символ найден — кодируется его вероятность и процесс завершается
    - Если символ не найден — кодируется вероятность escape (выхода к более короткому контексту)
    - При escape все символы текущего контекста добавляются в множество исключений (update exclusion)
    - Переход к контексту порядка N−1 и повторение

2. **Порядок −1 (fallback):**
    - Если символ не найден ни в одном контексте, он кодируется равномерным распределением
      среди оставшихся (не исключённых) символов из 256 возможных

3. **Обновление модели:**
    - После кодирования символа обновляются частоты во всех контекстах от порядка 0 до N
    - Путь контекстов сдвигается: для каждого порядка k новый контекст = старый контекст порядка k−1, расширенный
      текущим символом
    - При достижении суммарной частоты порога (8192) выполняется масштабирование — все частоты делятся пополам, нулевые
      записи удаляются

#### Метод D для оценки escape

Используется формула оценки вероятностей из метода D Говарда-Виттера:

- **Вероятность символа s:** `P(s) = (2*c(s) − 1) / (2*T)`, где `c(s)` — число наблюдений символа, `T` — суммарная
  частота
- **Вероятность escape:** `P(esc) = d / (2*T)`, где `d` — количество различных символов в контексте

Сумма вероятностей: `Σ(2*c(s)−1) + d = 2T − d + d = 2T` — корректное распределение.

По сравнению с методом C (Хаффмана-аналог), метод D присваивает более высокие вероятности частым символам
и более низкую вероятность escape, что улучшает сжатие на большинстве типов данных.

#### 2. Арифметическое кодирование

Range-кодер реализован по схеме Subbotin/Schindler с carry-propagation через кэш байтов:

- **Рабочий диапазон:** 32-битное целое `_range`, начальное значение `0xFFFFFFFF`
- **Нижняя граница:** 64-битное целое `_low` для обработки переноса (carry)
- **Нормализация:** при сужении диапазона ниже `2^24` — сдвиг и запись байта в выходной поток
- **Кэширование carry:** буфер из `0xFF` байтов для корректной обработки цепных переносов

Для каждого символа кодер выполняет:

```
range = range / totalFreq
low  += cumFreq * range
range = freq * range
normalize()
```

Где `cumFreq` — кумулятивная частота символов с меньшим значением, `freq` — частота данного символа,
`totalFreq` — суммарная частота всех символов (включая escape).

### Формат архива

В выходной файл последовательно записываются:

1. Размер исходного файла (`int32`, 4 байта)
2. Порядок контекстной модели (`byte`, 1 байт)
3. Арифметически закодированные данные (переменная длина)

Фиксированный overhead метаданных: **5 байт** на файл (по сравнению с 268 байт у Huffman-варианта).

## Декодер

### Процесс декодирования

Декодирование выполняется в обратном порядке с идентичной контекстной моделью:

1. Из архива считываются метаданные: размер оригинала (4 байта) и порядок модели (1 байт)
2. Инициализируется range-декодер: считываются первые 5 байт арифметического потока в рабочий регистр `_code`
3. Для каждого байта оригинала:
    - Строится то же множество контекстов, что и при кодировании
    - По пороговому значению `threshold = code / range` определяется, какой символ (или escape) закодирован
    - При escape — спуск к контексту меньшего порядка с тем же механизмом update exclusion
    - После определения символа — обновление модели (идентично кодеру)
4. Восстановленные данные записываются в выходной файл

Корректность декодирования гарантируется тем, что кодер и декодер поддерживают идентичное состояние
контекстной модели на каждом шаге — обе стороны выполняют одинаковые обновления статистики после каждого символа.

## Результаты сжатия

| File   | Initial size | Compressed size | Compressed (%) | File bytes |
|--------|--------------|-----------------|----------------|------------|
| bib    | 108.65 KB    | 27.60 KB        | 74.594         | 28267      |
| book1  | 750.75 KB    | 222.29 KB       | 70.391         | 227622     |
| book2  | 596.54 KB    | 154.20 KB       | 74.151         | 157901     |
| geo    | 100 KB       | 61.89 KB        | 38.105         | 63380      |
| news   | 368.27 KB    | 117.06 KB       | 68.213         | 119872     |
| obj1   | 21 KB        | 10.97 KB        | 47.740         | 11238      |
| obj2   | 241.03 KB    | 79.84 KB        | 66.874         | 81759      |
| paper1 | 51.92 KB     | 16.23 KB        | 68.736         | 16620      |
| paper2 | 80.27 KB     | 24.48 KB        | 69.506         | 25066      |
| paper3 | 45.44 KB     | 15.55 KB        | 65.785         | 15919      |
| paper4 | 12.97 KB     | 5.01 KB         | 61.388         | 5130       |
| paper5 | 11.67 KB     | 4.68 KB         | 59.905         | 4793       |
| paper6 | 37.21 KB     | 12.12 KB        | 67.427         | 12412      |
| pic    | 501.19 KB    | 51.42 KB        | 89.740         | 52655      |
| progc  | 38.68 KB     | 12.49 KB        | 67.724         | 12785      |
| progl  | 69.97 KB     | 16.28 KB        | 76.726         | 16675      |
| progp  | 48.22 KB     | 11.20 KB        | 76.765         | 11473      |
| trans  | 91.50 KB     | 18.57 KB        | 79.703         | 19017      |

> Суммарное значение сжатого размера всех файлов: **882 584 байт**
>
> Порядок контекстной модели: **6**
